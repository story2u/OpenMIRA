// Next.js shell for the phase-one frontend rewrite.
// It keeps routing local to / and /admin until API compatibility work begins.
const nextConfig = {
  poweredByHeader: false,
  reactStrictMode: true,
};

export default nextConfig;
